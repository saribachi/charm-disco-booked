server {
    listen       80;
    server_name  _;
    root   /usr/share/nginx/html;
    index  index.html;

    # Clean URLs: /thankyou-gtm serves thankyou-gtm.html (and .html still works)
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
